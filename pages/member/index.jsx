import Layout from '../../components/Layout'; import Protected from '../../components/Protected'; import { getSupabase } from '../../lib/supabaseClient'; import { createClient } from '@supabase/supabase-js';
export async function getServerSideProps(){ const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); const {data}=await s.from('member_dashboard_items').select('*').order('sort_order',{ascending:true}).order('created_at',{ascending:false}); return { props:{ items:Array.isArray(data)?data:[] } }; }
export default function Member({items}){
  const supabase=getSupabase();
  const login=async()=>{ const email=prompt('Enter your email for magic link'); if(!email) return; const {error}=await supabase.auth.signInWithOtp({ email, options:{ emailRedirectTo: (process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000')+'/member' } }); if(error) alert(error.message); else alert('Check your email for the login link.'); };
  return(<Layout title="Member"><Protected><div className="space-y-4"><div className="card"><h1 className="text-2xl font-bold">Member Dashboard</h1><p className="text-sm text-gray-600 mt-1">Access quick links and resources curated for members.</p><div className="mt-4 space-y-3">{items&&items.length?items.map(item=>(<div key={item.id} className="border border-gray-200 rounded p-3"><div className="font-semibold">{item.title}</div>{item.description&&<p className="text-sm mt-1">{item.description}</p>}{item.link_url&&<a className="btn btn-primary mt-3 inline-flex items-center" href={item.link_url} target="_blank" rel="noopener noreferrer">{item.link_label||'Open Link'}</a>}</div>)):<p>No member resources yet. Check back soon!</p>}</div></div><div className="card"><h2 className="text-xl font-semibold">Quick Actions</h2><a className="btn btn-primary mt-3" href="/member/announcements">View Announcements</a></div></div></Protected><div className="card mt-6"><h2 className="font-semibold">Not logged in?</h2><button className="btn btn-primary mt-2" onClick={login}>Email Magic Link</button></div></Layout>);
}
